import { Action } from 'rxjs/internal/scheduler/Action';
import { createAction, props } from '@ngrx/store';

export class DynamicStateActions{
    actions: [];

    constructor(actionEntity: string){
        
        const action = {}

        // this.actions = [...this.actions, action]


    }
}